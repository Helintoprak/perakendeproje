# Geliştirmede: boş bırak — Vite proxy (/api → http://127.0.0.1:5000/api) kullanılır.
# Üretimde: tam backend URL'i (ör. https://sporthink-api.onrender.com) — sonunda /api OLMAMALI.
VITE_API_BASE_URL=""

VITE_APP_NAME=Sporthink

# Stream Chat public API key (sadece API_KEY istemcide kullanılır, SECRET asla!)
# Backend'deki STREAM_API_KEY ile aynı değer olmalı.
VITE_STREAM_API_KEY="58km3bcrkpqe"
